{
  "name": "{{name}}",
  "version": "{{version}}",
  "description": "{{description}}",
  "main": "src/app.js",
  "scripts": {
    "test": "jest --watch",
    "dev": "cross-env NODE_ENV=dev nodemon",
    "prod": "cross-env NODE_ENV=prod pm2"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/lazyperson/quickstart-template.git"
  },
  "keywords": "{{keywords}}",
  "author": "{{author}}",
  "license": "{{license}}",
  "dependencies": {
    "ejs": "^3.1.5",
    "mysql": "^2.18.1"
  },
  "devDependencies": {
    "cross-env": "^7.0.2",
    "nodemon": "^2.0.5"
  }
}
